import styled from 'styled-components';

export const Container = styled.div`
   background: #fff;
   color: rgba(0,0,0,.85);
   padding: 14px;
   width: auto;
   height: auto;
   margin: 10px 20px;
`;